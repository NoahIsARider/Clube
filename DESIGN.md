# DESIGN.md

## 产品灵魂与意象锚点

产品名：**Clube** — 影协放映 / 艺术策展助手

意象锚点：**一间深夜放映厅的技术间**。金属操作台上贴着白色胶带标注按钮，胶片罐、监视器、示波器的荧光绿在幽暗中静静发光，一束投影机的光锥穿过灰尘颗粒落到银幕上。技术、克制、有质感，一种"电影从业者才懂的极客美学"，而不是流媒体那种炫目的红蓝渐变。

把产品名遮住，仅凭配色和布局用户应该联想到：胶片、老式电影海报网格、bauhaus 排版、终端窗口、Letterboxd 的克制感。

## 视觉策略

- 一切从"点、线、框、图片"四个基本元素组织：
  - **点**：单色小圆点作为状态灯（未开始 / 报名中 / 进行中 / 已结束 / 已归档），2–8px。
  - **线**：1px 细分割线组织信息层级，比 shadow 更"技术图纸"。
  - **框**：矩形边框、方形按钮、方形海报占位，不做过度圆角（radius 至多 4px，卡片主体 0–2px）。
  - **图片**：唯一允许"抢眼"的元素，海报大面积展示（aspect-ratio 2:3），周围留白喘息。
- 排版对齐参考"技术图纸 / 电影排期表"：等宽字体展示编号、时间、场地、票号；serif/sans 展示片名与短评。
- 图形语言：ASCII-ish 分隔符（`———`、`::`、`//`）在标题旁作为装饰；十字准星 `+` 作为空状态图案；页脚附小尺寸元信息（编号 / 时间戳 / 版本）。

## 配色方案（Design Tokens · 色彩）

采取"胶片黑 + 银幕白 + 荧光绿点缀 + 琥珀警告"的四色克制方案，避免行业默认的科技蓝紫。

- **背景 (Ink Black)** `#0B0B0C`：胶片罐哑黑，主背景。
- **前景 (Screen White)** `#F4F1EA`：老银幕暖白，主文本与卡片描边。
- **中灰 (Grain Grey)** `#3A3A3D`：分割线、次级文本、禁用态。
- **强调 (Phosphor Green)** `#B5FF3B`：CRT 荧光绿，主 CTA / 关键数据 / 状态灯"进行中"。
- **警告琥珀 (Amber)** `#FFB020`：报名截止、签到码即将过期。
- **胶片红 (Reel Red)** `#E5484D`：低使用率，仅用于删除 / 危险动作。

浅色模式作为兜底：背景 `#F4F1EA`，前景 `#0B0B0C`，强调色不变。默认交付**深色**为主视觉。

## 字体排版（Design Tokens · 字体）

- **主标题（片名、页面标题）**：`Instrument Serif` — 一款带电影海报气质的高反差衬线字，与"艺术策展"心智契合。
- **正文与 UI 文本**：`Inter` — 中性、可读性强。
- **等宽（时间、编号、票号、代码、签到码）**：`JetBrains Mono` — 极客气质，与技术图纸叙事一致。
- 中文字体族回退：`"PingFang SC", "Noto Sans SC", "Source Han Sans SC", system-ui`。
- 字号节奏：`12 / 14 / 16 / 20 / 28 / 44`，避免过多层级。行高：正文 1.6，标题 1.2。
- 字重：正文 400；小标签 / 元信息 500；主标题 400（衬线本身有分量，不用 700）。
- 排版细节：等宽字段全部大写 + `letter-spacing: 0.08em`（如 `NO.024 · 21:30 · HALL B`）。

## 布局与响应式

- 桌面：最大内容宽度 1200px，两栏（左侧栏 240px 导航，右侧主内容），底部固定 1px 分割线。
- 移动：底部 tab bar（首页 / 排片 / 签到 / 我的），单栏，海报网格 2 列。
- 间距节奏：4 / 8 / 12 / 16 / 24 / 32 / 48，遵循 4px 网格。
- 页面内所有容器边框统一 `1px solid #F4F1EA / 12%`，不用阴影。

## 组件规范

- **按钮**：矩形（radius 2px），主按钮 `Phosphor Green` 背景 + `Ink Black` 文字；次按钮透明 + 1px 边框；危险按钮 `Reel Red` 边框。禁止渐变。
- **卡片**：1px 边框、无阴影；hover 时边框色从 12% 提升到 40% 透明度，无位移。
- **状态灯**：`● ` 前缀小圆点 + 状态文本（等宽），颜色按状态映射。
- **海报占位**：`aspect-ratio: 2/3`，无内容时显示中央十字准星 `+` 与胶片齿孔上下装饰。
- **表单**：无背景色、底边 1px、focus 时底边变 `Phosphor Green`；label 位于输入框上方，等宽 12px 大写。
- **表格 / 排片表**：斑马线用 `#F4F1EA / 3%`，表头等宽大写小字，序号列右对齐。
- **签到码**：中央大尺寸单色 QR，四角带十字准星装饰，下方等宽显示 6 位备用数字码。

## 动效与交互

- 运动性格：**克制、机械、无弹性**。禁用 spring / bounce，全部 `cubic-bezier(0.4, 0, 0.2, 1)`，时长 150–250ms。
- 悬停：颜色变化 + 边框透明度变化，不做位移和放大。
- 过渡：页面切换用 opacity fade，无 slide。
- 加载：等宽字符扫描线 `[■■■□□□□]` 或旋转的胶片齿轮图标，不用彩色 spinner。
- 数据出现：数字采用"逐位跳动到目标值"的机械翻牌感（学期总结页专用）。

## 设计禁忌

- ❌ 不要使用蓝紫渐变、玻璃拟态、Neumorphism。
- ❌ 不要使用圆形按钮、大圆角卡片（>8px）、阴影浮起。
- ❌ 不要使用彩色 emoji 作为图标；图标一律 `lucide-react` 线性 1.5px。
- ❌ 不要满屏色块；除强调色外，任何一屏彩色区域面积不超过 5%。
- ❌ 不要 bounce / spring 动效、Lottie 彩色动画。
- ❌ 不要出现"AI 默认审美"：紫色霓虹、赛博朋克霓虹条、卡片渐变边框。
