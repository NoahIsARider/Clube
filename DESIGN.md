# Design System: Clube

## 1. 气质与意象
Clube 的界面不是传统校园活动管理后台，也不是大众化社交产品。它应当像一份为影展策展人准备的放映手册，带一点终端气息、档案馆秩序感和电影刊物的克制审美。整体气质偏冷静、理性、极简，但不能死板，要保留电影爱好者之间交流时的真实温度。

- 视觉密度：5/10。信息充足，但每个区域都保留呼吸感。
- 布局偏差：7/10。避免机械对称，允许标题、时间轴、状态标签形成轻微错位。
- 动效强度：4/10。只保留必要的浮现、滑入、聚焦反馈，不做炫技式动画。

关键词：
- 放映手册
- 片库档案
- 终端秩序
- 策展冷感
- 影迷温度

## 2. 视觉策略
整体采用“编辑型界面 + 轻终端质感”的混合策略。首页和组织页像独立电影刊物，活动流转页像一套精密的排片控制台。通过清晰的版式、时间信息、状态色和等宽辅助文本来建立专业感，而不是依赖夸张圆角、悬浮卡片和大面积渐变。

核心策略：
- 用版式层级建立高级感，不靠堆砌装饰。
- 用细边框、分隔线和留白替代重卡片容器。
- 用少量高辨识度强调色服务状态、按钮和选中反馈。
- 关键数据如时间、人数、场地、编号优先使用等宽字体。
- 页面尽量维持“一个主叙事区 + 一个辅助信息区”的结构。

## 3. 配色方案
- **Projection White** `#F5F3EE`：主背景，带一点胶片纸张感。
- **Archive Surface** `#EEEBE3`：次级背景，表格、侧栏、内嵌区域。
- **Reel Black** `#18181B`：主文字、主标题、重心元素。禁止使用纯黑 `#000000`。
- **Steel Gray** `#5F6368`：次级文字、说明、辅助标签。
- **Dust Line** `rgba(24, 24, 27, 0.12)`：分隔线、表单边框。
- **Festival Red** `#B6432A`：唯一强调色，用于主按钮、选中态、关键状态和图表点缀。
- **Screen Green** `#3F6B5B`：成功或已完成状态，仅用于语义提示，不参与品牌主视觉。

约束：
- 全站仅允许一个品牌强调色：`#B6432A`。
- 不使用紫蓝霓虹、荧光描边、玻璃拟态、AI 渐变大标题。
- 不做大面积高饱和色块铺底。

## 4. 字体排版
- **Display / Heading**：`Satoshi`, `Geist`, `PingFang SC`, `Microsoft YaHei`, sans-serif
- **Body**：`Geist`, `PingFang SC`, `Microsoft YaHei`, sans-serif
- **Mono / Data**：`JetBrains Mono`, `SFMono-Regular`, `Consolas`, monospace

规则：
- 标题字重偏中黑，字距略收紧，避免巨大标题压迫内容。
- 正文行宽控制在 `65ch` 以内，描述性文案保持克制。
- 时间、日期、场次编号、报名人数、反馈统计统一使用等宽字体。
- 移动端标题必须使用 `clamp()` 控制，避免一行只剩两三个字。
- 不使用 `Inter`。

## 5. 布局与交互
布局原则：
- 页面内容宽度使用容器约束，推荐 `max-width: 1200px`。
- 大屏使用 12 栏栅格，小屏全部折叠为单栏。
- 首页、排片页、活动详情页优先使用分栏结构，不使用三等宽功能卡片。
- 信息块之间通过分割线、标题、编号和留白分组，而不是到处套卡片。

交互原则：
- 主按钮为实底强调色，次按钮为线框或文本按钮。
- 激活态提供轻微位移或背景反差反馈，不使用外发光。
- 表单标签始终放在输入框上方，不使用悬浮标签。
- 加载态用骨架屏，不使用通用旋转小圆圈。
- 所有状态切换都要尽量即时反馈，例如“报名成功”“审核通过”“反馈已提交”。

关键页面导向：
- 首页：强调组织身份、近期放映、加入入口、最新影评。
- 组织控制台：聚焦成员、审批、邀请码、数据导出。
- 排片日历：突出日期、场地、片单状态和报名人数。
- 活动详情：突出影片信息、预告、报名、签到、反馈入口。
- 反馈复盘：突出匿名评分分布、关键词短评和导出操作。

## 6. 设计禁忌
明确禁止以下模式：

- 禁止通用 SaaS 风三列等宽卡片布局。
- 禁止过度圆角和漂浮阴影造成的“AI 模板感”。
- 禁止居中大标题加副标题再加双按钮的通用英雄区模板。
- 禁止霓虹光效、赛博紫蓝、玻璃态毛玻璃面板。
- 禁止为了“电影感”滥用胶片划痕、噪点背景和伪复古滤镜。
- 禁止过多拟物图标和插画喧宾夺主。
- 禁止使用假大空文案，例如“重新定义校园电影体验”。
- 禁止无层级的按钮海和状态标签海。
- 禁止移动端横向滚动。

## 7. 前端实现约束
- 优先使用 CSS Grid 和稳定的 Flex 布局，不使用脆弱的百分比 `calc()` 拼接。
- 动画仅允许作用于 `transform` 和 `opacity`。
- 全站优先浅底深字，夜间风格可后续扩展，不在 MVP 首版同时做双主题。
- 图像资源若需要占位，必须使用可替换的真实电影策展语境图，不得出现低质占位图。
- 所有页面必须保证 Vercel 部署稳定，并保留 GitHub Pages 静态部署兼容性。
